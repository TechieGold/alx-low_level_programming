#include <stdio.h>
#include <stdlib.h>

/**
  * main - A program that prints its name, followed by a new line.
  *
  * Return: 0 on success
  */

int main(int argc, char* argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

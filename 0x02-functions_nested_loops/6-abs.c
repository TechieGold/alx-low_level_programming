#include "main.h"

/**
  * _abs - Entry point
  * @int:
  * Description - Write a function that computes the absolute
  * value of an integer
  * Return: Always 0
  */

int _abs(int r)
{
	if (r > 1)
		return (r);
	else
		return (-r);

}
